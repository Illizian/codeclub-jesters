module.exports = {
  /*
   * add(a, b)
   - Math/1 - Return the value of a add b
   */
  add: function(a, b) {

  },

  /*
   * subtract(a, b)
   - Math/2 - Return the value of a subtract b
   */
  subtract: function(a, b) {

  },

  /*
   * multiply(a, b)
   - Math/3 - Return the value of a multiply b
   */
  multiply: function(a, b) {

  }
};