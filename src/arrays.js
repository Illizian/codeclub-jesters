module.exports = {
  /*
   * getItemAtIndex(array, index)
   - Array/1 - Return the item from the `array` at the `index`
   */
  getItemAtIndex: function(array, index) {

  },

  /*
   * findItemInArray(array, item)
   - Array/2 - Return the index of a string as `item` from the `array`
   */
  findItemInArray: function(array, item) {

  },

  /*
   * removeItemAtIndexInArray(array, index)
   - Array/3 - Remove the item at the `index` in `array`
   */
  removeItemAtIndexInArray: function(array, index) {

  },

  /*
   * sum(array)
   - Array/4 - Add up all the numbers in `array` and return the total
   */
  sum: function(array) {

  },

  /*
   * isBigger(array, min)
   - Array/5 - Remove all numbers in `array` that aren't larger than `min`
   */
  isBigger: function(array, min) {

  },

  /*
   * doubleItAll(array)
   - Array/6 - Double (x2) all numbers in the `array`
   */
  doubleItAll: function(array) {

  },

  /*
   * applyFn(array, fn)
   - Array/7 - Apply the `fn` function on every item in the `array`
   */
  applyFn: function(array, fn) {

  },

  /*
   * countOccurences(array, item)
   - Array/8 - Return the amount of times `item` occur in `array`
   */
  countOccurences: function(array, item) {

  },
};