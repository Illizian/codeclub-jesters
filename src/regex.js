/*
 * Use Regular Expressions to Pass These :)
 * http://www.regex101.com is your friend
 * (make sure it's in Javascript mode)
 */

module.exports = {
  /*
   * containsNumber(string)
   - Regex/1 - Return true if `string` contains numbers
   */
  containsNumber: function(string) {

  },

  /*
   * containsRepeatingLetter(string)
   - Regex/2 - Return true if `string` contains a repeating letter
   */
  containsRepeatingLetter: function(string) {

  },

  /*
   * endsWithVowel(string)
   - Regex/3 - Return true if `string` ends with a vowel (aeiou)
   */
  endsWithVowel: function(string) {

  },

  /*
   * captureThreeNumbers(string)
   - Regex/4 - Return the first series of three numbers
   */
  captureThreeNumbers: function(string) {

  },

  /*
   * matchesPattern(string)
   - Regex/5 - Return true if `string` matches pattern
   -           the pattern is 0XXXX XXXXXX where all X's are digits
   */
  matchesPattern: function(string) {

  },

  /*
   * isGBP(string)
   - Regex/6 - Return true if `string` contains correctly-formatted monetary amounts in GBP
   */
  isGBP: function(string) {

  }
};