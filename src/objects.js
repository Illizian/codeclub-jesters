module.exports = {
  /*
   * message(author, message)
   - Objects/1 - Return an object with the properties:
   -             date: The Date (see http://mdn.io/Date)
   -             author: The `author` value
   -             message: The `message` value
   */
  message: function(author, message) {

  },

  /*
   * getFullname(person)
   - Objects/2 - Generate a fullname string from the `person` object, it should take the form Mr Tim Smith.
   -             The `person` object contains the properties: title, firstname & surname that should be used
   -             to create the new string.
   */
  getFullname: function(person) {

  },

  /*
   * getPropertyValue(object, property)
   - Objects/3 - Return the value of the given `property` in `object`
   */
  getPropertyValue: function(object, property) {

  }
};