module.exports = {
  /*
   * truncate(string, length)
   - Strings/1 - Return `string` reduced to the the `length` specified
   */
  truncate: function(string, length) {

  },

  /*
   * contains(string, searchString)
   - Strings/2 - Return true if `string` contains `searchString`
   */
  contains: function(string, searchString) {

  },

  /*
   * append(string, appendString)
   - Strings/3 - Append `appendString` to `string` and return the new string
   */
  append: function(string, appendString) {

  },

  /*
   * prepend(string, prependString)
   - Strings/4 - Prepend `prependString` to `string` and return the new string
   */
  prepend: function(string, prependString) {

  }
};